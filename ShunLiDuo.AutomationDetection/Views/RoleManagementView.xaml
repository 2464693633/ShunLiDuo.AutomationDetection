<UserControl x:Class="ShunLiDuo.AutomationDetection.Views.RoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style x:Key="SearchButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AddButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LinkButtonStyle" TargetType="TextBlock">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="TextDecorations" Value="Underline"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="White" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tabs -->
        <TabControl Grid.Row="0" Margin="0,0,0,15" Style="{StaticResource StandardTabControlStyle}">
            <TabItem Content="角色列表" Style="{StaticResource StandardTabItemStyle}" IsSelected="True"/>
        </TabControl>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                    Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource StandardTextBoxStyle}"
                    Margin="0,0,10,0"/>
            
            <Button Grid.Column="1" Content="查询" Style="{StaticResource PrimaryButtonStyle}" 
                   Command="{Binding SearchCommand}"/>
        </Grid>

        <!-- Add Button -->
        <Button Grid.Row="2" Content="新增" Style="{StaticResource PrimaryButtonStyle}" 
               Command="{Binding AddCommand}" 
               HorizontalAlignment="Left" Margin="0,0,0,15"/>

        <!-- Data Grid -->
        <DataGrid Grid.Row="3" 
                 ItemsSource="{Binding Roles}"
                 Style="{StaticResource StandardDataGridStyle}"
                 Margin="0"
                 RowStyle="{StaticResource DataGridRowStyle}"
                 CellStyle="{StaticResource DataGridCellStyle}"
                 ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding Id}" Width="70" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                <DataGridTextColumn Header="角色编号" Binding="{Binding RoleNo}" Width="130" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                <DataGridTextColumn Header="角色名称" Binding="{Binding RoleName}" Width="180" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="编辑" Foreground="{StaticResource SuccessBrush}" 
                                          Style="{StaticResource LinkButtonStyle}"
                                           Margin="0,0,15,0"
                                          MouseLeftButtonDown="Edit_Click"/>
                                <TextBlock Text="删除" Foreground="{StaticResource DangerBrush}" 
                                          Style="{StaticResource LinkButtonStyle}"
                                            Margin="0,0,15,0"
                                          MouseLeftButtonDown="Delete_Click"/>
                                <TextBlock Text="查看" Foreground="{StaticResource InfoBrush}" 
                                          Style="{StaticResource LinkButtonStyle}"
                                          MouseLeftButtonDown="View_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <Grid Grid.Row="4" Margin="0,15,0,0" Height="40">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="{Binding TotalCount, StringFormat='共{0}条'}" 
                          VerticalAlignment="Center" 
                          FontSize="13"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Margin="0,0,20,0"/>
                <Button Content="&lt;" Style="{StaticResource PaginationButtonStyle}"/>
                <Button Content="1" Style="{StaticResource ActivePaginationButtonStyle}"/>
                <Button Content="&gt;" Style="{StaticResource PaginationButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

