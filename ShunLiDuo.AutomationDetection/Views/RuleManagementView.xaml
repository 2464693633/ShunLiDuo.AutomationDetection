<UserControl x:Class="ShunLiDuo.AutomationDetection.Views.RuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="White" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab -->
        <TabControl Grid.Row="0" Margin="0,0,0,15" Style="{StaticResource StandardTabControlStyle}">
            <TabItem Content="规则列表" Style="{StaticResource StandardTabItemStyle}" IsSelected="True"/>
        </TabControl>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                    Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource StandardTextBoxStyle}"
                    Margin="0,0,10,0"/>
            
            <Button Grid.Column="1" Content="查询" Style="{StaticResource PrimaryButtonStyle}" 
                   Command="{Binding SearchCommand}"/>
        </Grid>

        <!-- Add Button -->
        <Button Grid.Row="2" Content="新增" Style="{StaticResource PrimaryButtonStyle}" 
               Command="{Binding AddCommand}" 
               HorizontalAlignment="Left" Margin="0,0,0,15"/>

        <!-- Data Grid -->
        <DataGrid Grid.Row="3" 
                 ItemsSource="{Binding Rules}"
                 Style="{StaticResource StandardDataGridStyle}"
                 Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="规则编号" Binding="{Binding RuleNo}" Width="130"/>
                <DataGridTextColumn Header="规则名称" Binding="{Binding RuleName}" Width="160"/>
                <DataGridTextColumn Header="检测室" Binding="{Binding DetectionRooms}" Width="140"/>
                <DataGridTextColumn Header="物流盒编号" Binding="{Binding LogisticsBoxNos}" Width="160"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="编辑" Foreground="{StaticResource SuccessBrush}" 
                                          Style="{StaticResource LinkButtonStyle}"/>
                                <TextBlock Text="删除" Foreground="{StaticResource DangerBrush}" 
                                          Style="{StaticResource LinkButtonStyle}"/>
                                <TextBlock Text="查看" Foreground="{StaticResource InfoBrush}" 
                                          Style="{StaticResource LinkButtonStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <Grid Grid.Row="4" Margin="0,15,0,0" Height="40">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="{Binding TotalCount, StringFormat='共{0}条'}" 
                          VerticalAlignment="Center" 
                          FontSize="13"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Margin="0,0,20,0"/>
                <Button Content="&lt;" Style="{StaticResource PaginationButtonStyle}"/>
                <Button Content="1" Style="{StaticResource ActivePaginationButtonStyle}"/>
                <Button Content="&gt;" Style="{StaticResource PaginationButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

