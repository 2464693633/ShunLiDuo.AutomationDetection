<Window x:Class="ShunLiDuo.AutomationDetection.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ShunLiDuo.AutomationDetection.Converters"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="登录" Height="500" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:StringToVisibilityConverterForError x:Key="StringToVisibilityConverterForError"/>
            <converters:InvertBooleanToVisibilityConverter x:Key="InvertBooleanToVisibilityConverter"/>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Side: Branding / Image Area -->
        <Border Grid.Column="0" 
                CornerRadius="8,0,0,8" 
                Background="{DynamicResource PrimaryBrush}">
            <Border.Effect>
                 <DropShadowEffect Color="#1890FF" ShadowDepth="0" BlurRadius="20" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <!-- Decorative Circle -->
                <Ellipse Width="400" Height="400" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-100,-100,0,0">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFFFFF" Offset="0"/>
                            <GradientStop Color="#FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                    <Ellipse.Opacity>0.1</Ellipse.Opacity>
                </Ellipse>
                 <Ellipse Width="200" Height="200" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-50,-50">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFFFFF" Offset="0"/>
                            <GradientStop Color="#FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                    <Ellipse.Opacity>0.05</Ellipse.Opacity>
                </Ellipse>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="自动化检测系统" 
                               FontSize="36" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>
                    <TextBlock Text="Efficient · Intelligent · Reliable" 
                               FontSize="16" 
                               Foreground="White" 
                               Opacity="0.8" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Right Side: Login Form -->
        <Border Grid.Column="1" 
                Background="White" 
                CornerRadius="0,8,8,0">
             <Border.Effect>
                 <DropShadowEffect Color="#000000" ShadowDepth="0" BlurRadius="20" Opacity="0.1"/>
            </Border.Effect>
            <Grid Margin="60,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="欢迎登录" 
                               FontSize="26" 
                               FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimaryBrush}" 
                               Margin="0,0,0,40"/>

                    <!-- 账号 -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="账号" Style="{StaticResource StandardLabelStyle}" Margin="0,0,0,8"/>
                        <TextBox x:Name="LoginAccountTextBox"
                                Text="{Binding LoginAccount, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource StandardTextBoxStyle}"
                                Height="40"
                                FontSize="14"
                                KeyDown="LoginAccountTextBox_KeyDown">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </StackPanel>

                    <!-- 密码 -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="密码" Style="{StaticResource StandardLabelStyle}" Margin="0,0,0,8"/>
                        <PasswordBox x:Name="PasswordBox"
                                    PasswordChanged="PasswordBox_PasswordChanged"
                                    Height="40"
                                    FontSize="14"
                                    Padding="10,8"
                                    KeyDown="PasswordBox_KeyDown">
                            <PasswordBox.Resources>
                                <Style TargetType="PasswordBox">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="PasswordChar" Value="●"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="PasswordBox">
                                                <Border x:Name="border" 
                                                        Background="{TemplateBinding Background}" 
                                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                                        CornerRadius="4">
                                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsFocused" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="border" Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#1890FF" ShadowDepth="0" BlurRadius="4" Opacity="0.2"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </PasswordBox.Resources>
                            <PasswordBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                            </PasswordBox.InputBindings>
                        </PasswordBox>
                    </StackPanel>

                    <!-- 错误信息 -->
                    <Border Background="#FFF1F0"
                            BorderBrush="#FFA39E"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12,8"
                            Margin="0,0,0,20"
                            Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverterForError}}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AlertCircleOutline" 
                                                    Width="16" Height="16"
                                                    Foreground="{DynamicResource ErrorBrush}"
                                                    Margin="0,0,8,0"
                                                    VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ErrorMessage}" 
                                      Foreground="{DynamicResource ErrorBrush}"
                                      FontSize="13"
                                      TextWrapping="Wrap"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- 按钮 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                         <Button Content="取消" 
                                Command="{Binding CancelCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0" 
                                MinWidth="100"
                                Height="40"/>
                        <Button Command="{Binding LoginCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                MinWidth="120"
                                Height="40"
                                IsEnabled="{Binding LoginCommand.CanExecute}">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                                                Value="0"
                                                IsIndeterminate="True"
                                                Width="16" Height="16"
                                                Margin="0,0,8,0"
                                                Foreground="White"
                                                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <TextBlock VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="登录"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                        <Setter Property="Text" Value="登录中..."/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
