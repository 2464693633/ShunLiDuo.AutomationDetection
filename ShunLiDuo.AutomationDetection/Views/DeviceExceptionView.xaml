<UserControl x:Class="ShunLiDuo.AutomationDetection.Views.DeviceExceptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="White" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tabs -->
        <TabControl Grid.Row="0" Margin="0,0,0,15" Style="{StaticResource StandardTabControlStyle}">
            <TabItem Content="设备异常列表" Style="{StaticResource StandardTabItemStyle}" IsSelected="True"/>
        </TabControl>

        <!-- Filter Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="开始时间" Style="{StaticResource StandardLabelStyle}" Margin="0,0,8,0"/>
            <DatePicker Grid.Column="1" SelectedDate="{Binding StartTime}" Style="{StaticResource StandardDatePickerStyle}" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="2" Text="-" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14"/>
            <DatePicker Grid.Column="3" SelectedDate="{Binding EndTime}" Style="{StaticResource StandardDatePickerStyle}" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="4" Text="检测室" Style="{StaticResource StandardLabelStyle}" Margin="0,0,8,0"/>
            <ComboBox Grid.Column="5" SelectedItem="{Binding SelectedRoom}" Style="{StaticResource StandardComboBoxStyle}" Margin="0,0,10,0">
                <ComboBoxItem Content="全部"/>
            </ComboBox>
            <Button Grid.Column="7" Content="查询" Style="{StaticResource PrimaryButtonStyle}" 
                   Command="{Binding QueryCommand}" Margin="0,0,10,0"/>
            <Button Grid.Column="8" Content="导出" Style="{StaticResource SecondaryButtonStyle}" 
                   Command="{Binding ExportCommand}"/>
        </Grid>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2" 
                 ItemsSource="{Binding DeviceExceptions}"
                 Style="{StaticResource StandardDataGridStyle}"
                 Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="异常编号" Binding="{Binding ExceptionNo}" Width="130"/>
                <DataGridTextColumn Header="异常时间" Binding="{Binding CreateTime, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160"/>
                <DataGridTextColumn Header="异常设备" Binding="{Binding DeviceName}" Width="130"/>
                <DataGridTextColumn Header="检测室" Binding="{Binding RoomName}" Width="110"/>
                <DataGridTextColumn Header="异常代码" Binding="{Binding ExceptionType}" Width="110"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <Grid Grid.Row="3" Margin="0,15,0,0" Height="40">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="{Binding TotalCount, StringFormat='共{0}条'}" 
                          VerticalAlignment="Center" 
                          FontSize="13"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Margin="0,0,20,0"/>
                <Button Content="&lt;" Style="{StaticResource PaginationButtonStyle}"/>
                <Button Content="1" Style="{StaticResource ActivePaginationButtonStyle}"/>
                <Button Content="&gt;" Style="{StaticResource PaginationButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

